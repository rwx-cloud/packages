- key: base
  call: $LEAF_DIGEST

- key: test-user
  run: |
    cat /etc/os-release
    test "$(whoami)" == "ubuntu"
    test "$(sudo whoami)" == "root"

- key: test-docker
  docker: true
  run: docker run hello-world
